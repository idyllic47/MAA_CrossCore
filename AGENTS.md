### 通用规则

- 适时使用Codex自带的可视化能力、mermaid绘图等，以便于让用户更好地理解概念、作用、关系、流程、结构等。绘图类型选graph、flowchart、mindmap还是什么，由你自行决定。
- 英文术语解释硬性规则：Agent 只要在回复正文、总结、验证结果、变更说明、计划、报错分析中主动输出用户没有输入过的英文词、英文缩写或技术名词，第一次出现时必须紧跟括号中文解释，解释要像给小孩讲一样直白。包括但不限于：代码术语、专业名词、工具名、字段名、命令名、产品名、套餐名、英文缩写。示例：`KPI hint（提示关键指标怎么看）`、`technical id（系统内部识别编号）`、`Credits（账户里可消耗的额度）`、`Playwright（自动打开浏览器测试页面的工具）`。若没有解释，视为没有遵守本规则。
- 每个术语、概念、名词等只能下定义一次，后续其他文档只能声明其原文在哪，而不能再定义、改写或粘贴等。否则势必会造成文档歧义，进而导致代码业务逻辑混乱等严重问题。

### 代码相关

- 若处于Git代码仓库中，在代码修改前及修改过程中，必须随时智能执行commit、push等操作，保持本地与远端及时同步，以免用户无法精确回滚代码。
- - 复用优先，调研适度：简单任务直接做，复杂、重复或通用问题再限时搜索GitHub等成熟方案，既要避免重复造轮子，也要防止过度研究。比如用户想给应用添加个日志能力，你应当看有无开源高星或免费够用的日志方案，基于此改造适配，而不是直接自己写。


### 网络相关

- 本机使用 Clash Party（本机代理客户端），实际转发进程是 `mihomo.exe`（负责按规则转发网络流量的程序）。混合代理仅监听 `127.0.0.1:7890`，Windows 系统代理以及 `HTTP_PROXY`、`HTTPS_PROXY`（让命令行程序走代理的环境设置）均指向该地址。
- 下载任何内容时，如速度持续低于 1 MB/s 或下载失败，必须及时报告。先确认 Clash Party、`mihomo.exe` 和 `127.0.0.1:7890` 均可用，再只为当前命令设置上述代理；不得静默长时间重试，也不得依据旧配置名盲目切换配置。若确需切换，先读取 `C:\Users\admin\AppData\Roaming\mihomo-party\profile.yaml` 的实时 `current`（当前所选配置编号）及其对应名称，再说明原因和影响。

### 维护文档相关

- 文档直接写清“是什么、为什么、怎么做、怎样算完成”。能用普通中文说明时，不使用“门禁、终态、收敛、落点、闭环、链路、证据边界、能力边界”等抽象包装词。必须保留的技术名词，第一次出现时用一句普通中文解释。
- 维护文档时，不得“描述旧行为、强调现在不再怎样”，除非用户强制要求。这样写文档只会浪费上下文。

---
